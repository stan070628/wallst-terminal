# ================================================================
# AI Stock Analysis Bot - Requirements
# Python 3.8 이상 필수
# ================================================================

# ---- UI 프레임워크 ----
streamlit>=1.28.0,<2.0.0              # 웹 대시보드 및 UI

# ---- 데이터 수집 & 처리 ----
yfinance>=0.2.32                       # 글로벌 주식 데이터 (나스닥, 암호화폐)
FinanceDataReader>=1.9.6               # 한국 주식 데이터 (KOSPI, KOSDAQ)
pandas>=2.0.0,<3.0.0                   # 데이터 분석 및 조작
numpy>=1.24.0,<2.0.0                   # 수치 계산

# ---- 시각화 ----
plotly>=5.17.0                         # 인터랙티브 차트 (Streamlit 통합)
matplotlib>=3.7.0                      # 기본 그래프 (백업 용도)

# ---- 기술 지표 분석 ----
# ta-lib 설치가 어려운 경우 ta (TA-Lib) 사용
# ta-lib: https://github.com/mrjbq7/ta-lib (C 컴파일 필요)
# ta: pandas-ta를 기반한 순수 Python 구현
ta>=0.11.0                             # 기술 지표 (VWAP, RSI, MACD, Ichimoku 포함)

# ---- 환경 변수 & 설정 ----
python-dotenv>=1.0.0                   # .env 파일에서 환경 변수 로드

# ---- HTTP 요청 ----
requests>=2.31.0                       # API 호출 및 웹 요청

# ---- 로깅 & 모니터링 (선택) ----
# logging은 Python 표준 라이브러리에 포함되어 있음
# 필요 시 다음 추가: python-json-logger>=2.0.7

# ================================================================
# 개발 환경 의존성 (선택사항)
# 설치: pip install -r requirements.txt extras/dev
# ================================================================

# ---- 테스트 ----
# pytest>=7.4.0                        # 단위 테스트
# pytest-cov>=4.1.0                    # 테스트 커버리지

# ---- 코드 품질 & 포매팅 ----
# black>=23.10.0                       # 코드 포매팅
# pylint>=3.0.0                        # 코드 정적 분석
# flake8>=6.1.0                        # 스타일 가이드 검사

# ---- 타입 체크 ----
# mypy>=1.6.0                          # 정적 타입 검사

# ================================================================
# 설치 명령어
# ================================================================
# 기본 설치: pip install -r requirements.txt
# 개발 환경: pip install -r requirements.txt -r requirements-dev.txt
# ================================================================